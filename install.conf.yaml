- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.Xmodmap: Xmodmap
    ~/.xmonad/xmonad.hs: xmonad.hs

- shell:
  - [git submodule update --init --recursive, Installing submodules]
